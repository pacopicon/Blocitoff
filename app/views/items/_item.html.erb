<%= content_tag :div, class: 'media', id: "item-#{item.id}" do %>
  <small><%= item.name %> (created <%= time_ago_in_words(item.created_at) %> ago) | <%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></small>
<% end %>
